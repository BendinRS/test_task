---
- name: Getting start date
  shell: date +%b%Y%H%M
  register: start_date

- debug:
    var: "{{ start_date.stdout }}"
- name: show templating results
  debug:
   msg: "{{ lookup('template', 'test.j2') }}"